# Load Build Cache

Loads the build cache from disk, or creates empty cache if none exists

## Usage

``` r
load_build_cache(cache_file)
```

## Arguments

- cache_file:

  Path to cache file

## Value

List with datasets, models, version, and build_params
