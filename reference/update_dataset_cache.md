# Update Dataset Cache

Updates cache with current dataset hashes

## Usage

``` r
update_dataset_cache(datasets, cache, new_ids, changed_ids)
```

## Arguments

- datasets:

  Named list of datasets

- cache:

  Build cache object

- new_ids:

  Vector of new dataset IDs

- changed_ids:

  Vector of changed dataset IDs

## Value

Updated cache object
