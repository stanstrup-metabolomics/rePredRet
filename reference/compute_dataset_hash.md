# Compute Hash for Dataset RT Data

Creates an MD5 hash of a dataset's RT data to detect changes

## Usage

``` r
compute_dataset_hash(dataset)
```

## Arguments

- dataset:

  Dataset object with rtdata component

## Value

Character string with MD5 hash
