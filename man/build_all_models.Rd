% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_building.R
\name{build_all_models}
\alias{build_all_models}
\title{Build All Pairwise Models}
\usage{
build_all_models(
  report_data,
  min_compounds = 10,
  n_boot = 200,
  alpha = 0.05,
  n_cores = parallel::detectCores(),
  method_match = TRUE,
  export_dir = NULL,
  method = c("fast_ci", "bootstrap"),
  save_plots = TRUE
)
}
\arguments{
\item{report_data}{Data from \code{load_report_data()}.}

\item{min_compounds}{Minimum common compounds required (default 10).}

\item{n_boot}{Number of bootstrap iterations (default 1000).}

\item{alpha}{Significance level for CI (default 0.01).}

\item{n_cores}{Number of CPU cores for parallel processing.}

\item{method_match}{If TRUE (default), only build models between
same method types (RP-RP, HILIC-HILIC).}

\item{export_dir}{Optional directory to incrementally save models as
they're built. If NULL (default), models are only stored in memory.}

\item{save_plots}{Logical, whether to save interactive HTML plots for each
model (default TRUE).}
}
\value{
A list with:
\describe{
\item{models}{Named list of model objects}
\item{index}{Tibble with model index and statistics}
}
}
\description{
Builds models between all pairs of chromatographic systems that share
sufficient common compounds.
}
