% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prediction.R
\name{predict_rt}
\alias{predict_rt}
\title{Predict RT for a Single Compound in a Target System}
\usage{
predict_rt(
  compound_id,
  target_system_id,
  models,
  datasets,
  ci_width_limit = .prediction_defaults$ci_width_limit,
  ci_width_limit_rel = .prediction_defaults$ci_width_limit_rel,
  density_bw_mult = .prediction_defaults$density_bw_mult,
  density_limit = .prediction_defaults$density_limit,
  id_column = "inchi.std"
)
}
\arguments{
\item{compound_id}{Compound identifier (InChI or InChIKey).}

\item{target_system_id}{Target system identifier.}

\item{models}{Named list of models from \code{build_all_models()}.}

\item{datasets}{Named list of datasets from \code{load_report_data()}.}

\item{ci_width_limit}{Maximum CI width in minutes (default 2.0).}

\item{ci_width_limit_rel}{Maximum relative CI width (default 0.2 = 20\%).}

\item{density_bw_mult}{Bandwidth multiplier for density check (default 0.03).}

\item{density_limit}{Minimum density threshold (default 0.01).}

\item{id_column}{Column name for compound matching (default "inchi.std").}
}
\value{
A list with prediction details including provenance, or NULL
if no valid prediction can be made.
}
\description{
Predicts the retention time of a compound in a target chromatographic
system using available models.
}
\details{
The function:
\enumerate{
\item Finds all models that predict TO the target system
\item Checks if the compound exists in any source system
\item Makes predictions and applies quality filters:
\itemize{
\item CI width must be < ci_width_limit OR < ci_width_limit_rel
\item Source RT must be in a dense region of calibration data
}
\item Returns the best prediction (narrowest relative CI)
}
}
